# 05 — Fixtures and Tags
# Demonstrates: name (custom test name), tags, use (viewport, locale)

name: Mobile inventory browsing
tags:
  - smoke
  - mobile

use:
  viewport:
    width: 375
    height: 812
  locale: en-US

goal: Verify inventory works on a mobile viewport

statements:
  - URL: /inventory.html

  - VERIFY: The inventory page is displayed
    js: "await expect(page.getByText('Products')).toBeVisible()"

  - VERIFY: The page layout is usable on mobile

  - intent: Add Sauce Labs Backpack to cart
    js: "await page.getByRole('button', { name: 'Add to cart' }).first().click()"

  - intent: Click the shopping cart icon
    js: "await page.locator('[data-test=\"shopping-cart-link\"]').click()"

  - VERIFY: The cart contains the selected product
    js: "await expect(page.locator('.cart_item')).toHaveCount(1)"
