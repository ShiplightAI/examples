{
  "mcpServers": {
    "shiplight": {
      "command": "npx",
      "args": [
        "-y",
        "@shiplightai/mcp@latest"
      ],
      "env": {
        "PWDEBUG": "console",
        "SHIPLIGHT_REGISTRY_URL": "${SHIPLIGHT_REGISTRY_URL:-http://127.0.0.1:17716/registry}"
      }
    }
  }
}
