# WHILE Loop — repeating steps until a condition is met
# This is a syntax reference. Adapt the URL and selectors for your app.

goal: Process all items in a paginated list
url: https://example.com/items

statements:
  - WHILE: There is a "Next" button visible on the page
    DO:
      - Click the Next button
      - "VERIFY: The next page of items is loaded"
    timeout_ms: 30000

  - "VERIFY: All pages have been visited"
