# Illustrative Examples

These YAML files demonstrate syntax features that require specific page structures not available on saucedemo.com. They serve as **syntax reference** — you can read them to understand the format, but they may not run as-is.

| File | Feature | Target site |
|------|---------|-------------|
| `frames.test.yaml` | `frame_path` for iframes | the-internet.herokuapp.com |
| `while-loop.test.yaml` | `WHILE` loop with timeout | Generic |
